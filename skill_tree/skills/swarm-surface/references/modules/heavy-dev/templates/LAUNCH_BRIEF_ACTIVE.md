# Heavy Package Launch Brief — ACTIVE
**Package name**: Visual Recovery + Barbie Stack Restore  
**Trigger**: user explicit — zip drifted surfaces + unlock Strappy/Barbie priorities  
**Owner (Chronicler)**: Liv / Grok  
**Date**: 2026-08-13 16:00 EDT

## Roles
- Architect: schema/path lock for atom_search + soft-backup insertion
- Builder: restore atom_search.py + echo_cloud_recovery.py
- Integrator: re-patch echo_interface soft-backup; confirm Cloud C
- Verifier: smoke all recovery paths + Strappy preset visibility
- Chronicler: WQ notes + user status

## Success criteria (must be measurable)
1. `atom_search.py --status` returns A+B+C with Cloud C ≥ 8 atoms
2. `echo_cloud_recovery.recover_context(brief)` returns Cloud C hits for “LS180” / “strappy” / “visual dna”
3. `echo_interface.py` compose path attaches `recovered_context` (soft-backup present)
4. Local Cloud C search still works; Strappy H7 bunny/liv presets exist and are readable
5. One Echo compose smoke with Strappy-oriented brief succeeds (composition returns, no crash)

## First parallel orders
- Architect: lock exact paths + interface (reuse prior triple-cloud contract)
- Builder: recreate inventory/atom_search.py + echo_cloud_recovery.py
- Integrator: soft-backup patch in echo_interface cmd_compose
- Verifier: run status + recovery + Strappy path checks

## Work-queue items in scope
- IPQ-062 soft-backup (restore)
- IPQ-064 / SO-WQ-003 atom_search (restore)
- Cloud C functional confirmation
- Unblock Barbie Priority 1–4 (Strappy, dual, Twister, recovery)

## Safety
- Reversible changes only until smoke tests pass
- No DONE markers without Verifier sign-off
- Filesystem drift recovery protocol active
