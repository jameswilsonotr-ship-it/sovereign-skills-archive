# Protocol — Option E: Minimal
**Intent:** Cleanest path — minimal prompt chrome, still DNA locked.

Generate engine: pure generate minimal prompts.
Overlay engine: minimal edit path (or generate-only mirror if edit is specialized — follow engine SKILL fork rules).

## Steps
1. Load display + scoring.
2. Emit minimal clean Liv + Bunny (or scoped set).
3. Score; update menu.
