# Split Engine

**Owner:** image-pipeline
**Status:** LIVE-DRAFT 2026-08-28 20:15 EDT
**Not a new top-level skill.**

Inbound still → segment → **four plates** (realistic + heat + anime + rig).
Heat and anime are both mandatory.

Twins:
- generate-engine = from-scratch `generate_image` (default inbound path C)
- overlay-engine = `edit_image` merge / Twister (path D, needs kept file)

See `VOCAB.md` and `PROTOCOL.md`.
