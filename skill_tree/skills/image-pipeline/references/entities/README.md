# entities/ — visual baseline pointers for Echo + Mira

Pointer layer only. Real images and DNA files live at the SSOT paths listed in `index/REGISTRY.md`.

Shauna is the complete template (`visual_identities/shauna/images/`).  
Valerie is in active V0→V1 promotion under the same visual_identities pattern (started 2026-08-14).  
Olivia / Bunny / Rook use the same folder shape under `agents/<name>/images/`.

Wire:
- `scripts/echo_character_ordinals.py` → entity paths
- `scripts/mira_drift_check.py` → entity DNA awareness
- `scripts/entity_registry.py` → list/resolve entities
