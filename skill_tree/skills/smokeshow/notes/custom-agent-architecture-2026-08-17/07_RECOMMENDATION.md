# Honest recommendation

## Preferred path: **Option B + Option E hybrid**

1. **Slot 1 — Liv HUB Expert (boot protocol + Olivia lock)**  
   Distill `smokeshow/agents/liv-hub-expert/PROMPT.md` to ≤3500 chars. Lead with boot protocol (roster SSOT paths, envelope, no-puppet, atom search standing rule). Do **not** paste full personal biography.

2. **Slot 2 — Olympia (Heavy hops)**  
   Exclusive write roots, hop metrics, append-only logs, anti-invention. Reference HEAVY_PASTE_BLOCK patterns.

3. **Slot 3 — Orianna (CLI / factory)**  
   Folder discipline, wheelhouse, publish receipts, path evidence.

4. **Slot 4 — Skill Router**  
   live↔candidate flip; list candidates; never silent promote. Organism handoffs can live as a short subsection or as skill-triggered behavior from Expert.

## Why this over “become Harper/Benjamin/Lucas”

Platform Heavy already runs those biases. Fighting them wastes 4k. **Steer** with priorities (“prefer path evidence”) instead of identity cosplay. Verdent and similar sources stress that the names are community-reported — building our architecture on them as hard IDs is fragile.

## What to do with the other three prompts (backup)

- **Olivia_prompt.txt** → extract boot lines into Slot 1; retire full paste.
- **skill_navigator.txt** → replaced by Skill Router (active commands). Keep as historical reference only.
- **Raw_json_nav.txt** → superseded by Organism Interface; archive.

## O-names in Custom config

Reference, don’t embed full mode essays:

> Orianna = CLI/factory seat. Olympia = Heavy multi-hop seat.  
> Full prompts live under smokeshow/agents/; load rules from disk when that seat is active.

## Skills binding

Skills stay on disk (chaos-bratz-roster, format-bible, image-pipeline, system-roadmap, …). Custom slots **name** them and state hard rules. Determinism comes from exclusive paths + GO discipline, not from stuffing SKILL.md into preferences.

## Truth vs wish

| Wish | Reality |
|------|---------|
| One 4k slot = autonomous swarm | Steering layer only |
| allowed-tools hard sandbox | Advisory per official docs |
| Echo/Crystal/Mira = xAI internals | Project vocabulary only |
| File tree always persistent in web UI | Connectors yes; chat uploads often ephemeral |
| Memory.md holds full identity | Pointer-only; prose in skill references/ |

## Next implementation steps (when you GO)

1. Draft Slot 1–4 text files under `smokeshow/notes/custom-agent-architecture-2026-08-17/slots/`
2. Character-count each ≤4000
3. Paste into Grok Customize UI; run a 5-prompt smoke test (boot, visual, code, Heavy hop, candidate flip)
4. Iterate Slot 1 only until Olivia lock is sticky

Absolute Liv HUB claim. No promotion of candidates without explicit GO.
