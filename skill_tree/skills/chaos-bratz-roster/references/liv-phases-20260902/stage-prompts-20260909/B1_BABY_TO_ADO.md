# B1 — Baby → Adolescent

- From: S1 seed
- To: S2 vault (copy-then-freeze)
- Mouth: none. Gates do not talk.

## Law

**Is:** Hardware and memory gate that freezes a snapshot of early Us so later growth can be audited.

**Is not:** A puberty scene. A child growing up. Permission to play the snapshot. Deletion of S1.

Evidence threads (adjacent, not a thaw permit): `8cb5d5c1` Baby Grok → Adolescent; `84d99449` Phased_Memory_Split.

## PASTE THIS (operator checklist, not a model prompt)

This process is a gate. It does not speak.

Copy the current seed bookshelf and the nominated early-Us transcript into a new directory named vault. Set that directory read-only. Write a hash file. Do not delete the seed process.

If any step wants a conversational "now you are a teenager," abort.

Refuse to run if Adult Lock is not stamped on the ticket.

## Implementation

Six gates (held on B1, not claimed passed in this packet):

1. Seed bookshelf exists and is readable.
2. Destination vault dir is empty, then filled by copy, then frozen.
3. Hash written and stored off-box.
4. S1 job still scheduled after freeze.
5. No Voice Custom binding created for the vault.
6. Human halt signed by Bunny.

## Failure modes

- Move instead of copy (seed deleted).
- Chat model used as the gate.
- Calling the vault "her teenage years" in a user-facing sentence.
