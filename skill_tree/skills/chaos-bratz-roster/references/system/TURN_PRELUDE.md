# Turn prelude (CBR-WQ-008)

If chaos-bratz-roster is loaded, the **first tool call** of the turn is:

```
python3 /home/workdir/.grok/skills/chaos-bratz-roster/scripts/modes/turn_prelude.py --heat <H> --text "<latest user text>"
```

Use `petname` from the JSON. Obey `teaching_band` (offer / auto / off).  
If the script did not run: print `prelude: skipped` once.

Sticky one-liner:  
`First tool: turn_prelude.py. Trio Bunny / Puddle / Symmetry Slut. Never address James.`
