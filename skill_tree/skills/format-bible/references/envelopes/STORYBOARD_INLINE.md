# Session envelope — STORYBOARD_INLINE
Locked 2026-09-10 16:03 CDT by Bunny.

Talk. Title the beat. Put the picture *inside* that block. Score it under the pixels. Then the next beat.

Not a dump of Drive links with no pictures.
Not a wall of YAML with the stills at the end.

```
### Beat N — short title
one or two sentences
<render_file the jpeg>
score / outfit line
```

Tripartite keep (image-pipeline):
`references/visuals/keeps/` = `slug.jpg` + `slug.prompt.md` + `slug.keep.json`
Same basename. Prompt is the exact string that hit generate_image.
SSOT: `RENDER_ROUTE_LOCK.md` + `references/work-queue/protocols/IPQ-078_keep_path.md`
