# FBQ-014 — YAML is harvest; dashboard is engine_state

**Status**: OPEN  
**Parent**: SR-WQ-071  
**Home**: format-bible

## Do
1. Document: YAML fields never include Heat/Filth (schema rule 4 already).
2. Dashboard line = `engine.py --thin` only.
3. Optional helper prints both blocks from one JSON so the model copies instead of invents.
4. Still FBQ-013: delete [TOP]/[BOTTOM] from SKILL.md Core Rules.

## Pointers
- `references/ENVELOPE_SCHEMA.md`
- `scripts/engine.py`
- `skill-orchestrator/references/debug/envelope_harvest.py`
