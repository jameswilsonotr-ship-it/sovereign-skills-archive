# OpenSpec

OpenSpec changes are kept under `openspec/changes/`. Each change has one
canonical directory whose name is its change ID.

The directory contract is documented in
[`openspec/changes/README.md`](changes/README.md). New changes must use that
contract so that the change ID, filesystem path, and references stay aligned.
