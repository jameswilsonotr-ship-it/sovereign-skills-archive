# Tasks

- [ ] Adopt lowercase kebab-case for every new directory directly under
  `openspec/changes/`.
- [ ] Keep each change directory name identical to its `Change ID`.
- [ ] Use lowercase kebab-case for capability directories under `specs/`.
- [ ] Preserve one canonical directory per change and avoid aliases.
- [ ] Review future OpenSpec changes against
  `openspec/changes/README.md`.
