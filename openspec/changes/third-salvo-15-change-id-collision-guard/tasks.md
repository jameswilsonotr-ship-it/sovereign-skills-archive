# Tasks

- [x] Reserve the change ID `third-salvo-15-change-id-collision-guard`.
- [x] Document the canonical collision comparison and stable outcomes.
- [x] Add an offline fixture covering exact collision, case-only collision,
  unique ID, and On-Demand rejection.
- [x] Add a receipt describing the atomic, docs-only scope and hard fences.
- [ ] Integrate this contract into a future OpenSpec validator, if one is
  introduced. This change intentionally provides no validator implementation.
