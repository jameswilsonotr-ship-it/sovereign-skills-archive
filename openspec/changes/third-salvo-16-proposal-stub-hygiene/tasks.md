# Tasks

- [x] Record the T3-16 scope and the `INCLUDED Ultra ONLY` / `NEVER`
  boundary in the proposal.
- [x] Define the required parked-ID stub metadata.
- [x] Define inertness, identity, and duplicate-review requirements.
- [ ] Apply the metadata contract to the authoritative parked-ID list in a
  separately approved implementation change.
- [ ] Add an offline validator for canonical IDs and required metadata when
  the parked-ID list has an executable home.

This change intentionally stops at proposal and specification material.
