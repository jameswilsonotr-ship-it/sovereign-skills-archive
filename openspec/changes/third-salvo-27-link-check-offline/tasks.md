# Tasks

- [ ] Add the offline checker to the owning validation tool.
- [ ] Parse inline, reference-style, and image Markdown links with source
  locations.
- [ ] Resolve only local paths relative to the source document.
- [ ] Validate heading slugs and explicit HTML IDs for local fragments.
- [ ] Sort findings and implement exit statuses `0`, `1`, and `2`.
- [ ] Keep the capability gated to `INCLUDED` / `Ultra`; add a rejection test
  for `On-Demand`.
- [ ] Run the script note's smoke examples from a disconnected workspace.
