# Tasks

- [x] Record T4-02 as the `CinC-TARGET` / `Van Clief keeper`.
- [x] Define `CONTINUOUS` reload in the `INCLUDED` lane.
- [x] State that On-Demand is forbidden and never emitted.
- [x] Add the offline-only scope and hard fences.
- [x] Add the single atomic receipt.
- [ ] Obtain review and merge the change.
