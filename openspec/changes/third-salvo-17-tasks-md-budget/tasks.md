# Tasks

> Size budget: at most 1,024 UTF-8 bytes and 24 non-empty lines. Check with
> `wc -c -l` locally; keep this list offline-only and Included Ultra-only.

- [ ] Record the eligibility and On-Demand exclusion in the spec.
- [ ] Keep this file within the stated byte and line limits.
- [ ] Validate the change locally without adding another execution mode.
